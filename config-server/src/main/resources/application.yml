server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yukismimi/cloud.git
#          username:
#          password:
          search-paths: config-repo
  application:
    name: config-server

